# 变更记录（本会话）

项目谱系：`@sanqi-normal/dsh-webui-market-plugin`（原版）→ `condaThinker/dsh-webui-market-plugin-plus`（fork：后台进度/可终止/启停）→ **本项目 dsh-market-github**（`loker66fan/dsh-market-github`，改名 + 启动页 + GitHub 实时搜索 + agent 工具等）。

## 时间线

1. **fork 起步**（提交 `4b9991b`）：attribution 归原版作者 Sanqi-normal；仓库指向本 fork。
2. **启动页入口 + GitHub 实时搜索 + 分页 + 一键重启**（`c4780de`）：`MarketOnboarding` 注册 `settings.onboarding`；`searchGitHub` 代理 GitHub Search API；prev/next 分页 50/页；host `restart` 路由 + 「立即重启」按钮。
3. **打包 v0.3.0**（`7f684c7`）：发布就绪 README/打包。
4. **改名 dsh-market-github + topics 渲染**（`c88bfbb`）。
5. **npm 优先安装 + 卡片质量信号 + embedded 启动页 + 配额显示 + CI**（`60243df`）：`npmRegistrySpec`（防抢注校验）、lang/license/更新时间渲染、`MarketPanel embedded`、`rate` 配额显示。
6. **诚实热挂载 + allowBuilds 同意**（`e35995b`）：web-client 半需重启不再谎报热挂载；验证通过的 git 包精确放行构建。
7. **op 轮询冻结修复**（`0f32140`）：任务状态丢失时提示刷新而非冻结在 running。
8. **代理/网络可靠性 + codeload 安装**（`6364702`）：codeload tarball、精确 allowBuilds 键、纯客户端插件合成行（`ensureClientRow`）。
9. **本机代理自动探测 + agent 模型工具四件套**（`7901d5f`，四项完善核心提交）：
   - `market_search/market_install/market_installed/market_update` 模型工具；
   - `resolveInstallSpec` 抽取（路由与工具共用安全闸）；
   - `listInstalled` 内置/后装投影 + `InstalledPanel`「已安装」标签页；
   - `checkSelfUpdate` 市场自更新横幅；
   - `MARKET_PER_PAGE` 分页可配置（`config.perPage`）。
10. **启动页不再每次弹出**（`fb23c4c`）：`MarketOnboarding` localStorage `mktsOnboardingDone` 持久化，已看过自动跳过。

## 会话内验证记录

- `npm run build` / `npm test` 全程绿灯（新增测试：searchGitHub/mapGitHubItem、listInstalled、resolveInstallSpec、checkSelfUpdate、detectProxy 等）。
- 运行时冒烟：`DSH_HOME=.dsh-dev` 源码 dsh web 于 3081 端口，日志确认工具注册，读图/搜索/分页实测可用。

## 部署快照（2026-08-15）

- 运行实例 1（3080）：npx 版 dsh + modlens 视觉插件（`~/.dsh`）。
- 运行实例 2（3081）：源码版 dsh（`deepseek-harness-master/.dsh-dev`），挂本市场插件（link）+ modlens + 透明主题。
- 源码：`deepseek-harness-master`（dsh rc.5）；本项目路径：`/home/loker/Desktop/测试项目/商城插件`。
